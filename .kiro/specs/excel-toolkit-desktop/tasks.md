# 实施计划: Excel 工具箱桌面应用

## 概述

本实施计划将 Excel 工具箱的设计转换为可执行的开发任务。采用增量开发方式，优先实现核心功能，然后逐步添加高级功能。每个任务都引用了相应的需求，确保可追溯性。

## 任务列表

### 阶段 1: 项目基础设施

- [x] 1. 搭建项目结构和开发环境
  - 创建项目目录结构（前端 Electron + Vue.js，后端 Python）
  - 配置 TypeScript 和 Python 开发环境
  - 设置 package.json 和 requirements.txt
  - 配置 ESLint、Prettier（前端）和 Black、Flake8（后端）
  - 设置 Git 仓库和 .gitignore
  - _需求: 9, 10_

- [x] 2. 实现前后端通信机制
  - [x] 2.1 实现 Python CLI 路由器
    - 创建命令行接口，监听 stdin 并解析 JSON 命令
    - **实现长连接模式**：Python 进程在 Electron 启动时启动，保持运行直到应用关闭，避免频繁启动/关闭进程
    - **使用 sys.stdin.readline() 循环读取命令**，而非一次性读取
    - **将所有日志输出到 stderr**：使用 `sys.stderr.write()` 而非 `print()`，避免干扰 stdout 中的 JSON 数据
    - 实现命令路由逻辑，根据 action 分发到相应模块
    - 实现 JSON 响应封装和错误处理
    - _需求: 系统架构_
    - _最佳实践: 长连接模式提升性能，日志分离避免数据污染_
  
  - [x] 2.2 实现 Electron 主进程的 Python 桥接
    - 使用 child_process 启动 Python 子进程
    - 实现 JSON 命令发送和响应接收
    - **实现实时进度更新机制**：监听 Python 的 stdout 流式输出，接收进度 JSON（如 `{"progress": 45}`），避免主进程同步等待
    - 实现超时处理和进程管理
    - 区分进度消息和最终结果消息（通过 `type` 字段：`progress` 或 `result`）
    - _需求: 系统架构_
    - _最佳实践: 避免长时间操作导致 UI 假死_
  
  - [ ]* 2.3 编写通信机制的集成测试
    - 测试命令发送和响应接收
    - 测试错误传递机制
    - 测试进程异常处理
    - _需求: 系统架构_

- [x] 3. 检查点 - 确保前后端通信正常
  - 确保所有测试通过，询问用户是否有问题

### 阶段 2: 核心文件操作

- [x] 4. 实现文件加载模块
  - [x] 4.1 实现 File_Loader 核心功能
    - 使用 openpyxl 加载 .xlsx 文件
    - 使用 xlrd 加载 .xls 文件
    - 实现文件格式验证
    - **实现文件占用检测**：捕获文件被其他程序（如 Excel）占用的异常，返回友好的错误提示 "请先关闭 Excel 程序"
    - 实现流式读取模式（大文件优化）
    - _需求: 1.1, 1.2, 1.7_
    - _最佳实践: 防止文件占用冲突_
  
  - [ ]* 4.2 编写文件加载的属性测试
    - **属性 1: 文件加载往返一致性**
    - **验证: 需求 1.1**
    - _需求: 1.1_
  
  - [ ]* 4.3 编写文件加载的边界测试
    - 测试文件不存在的情况（FILE_NOT_FOUND）
    - 测试文件损坏的情况（FILE_CORRUPTED）
    - 测试格式不支持的情况（FILE_FORMAT_UNSUPPORTED）
    - 测试权限不足的情况（FILE_PERMISSION_DENIED）
    - _需求: 1.3, 1.4, 1.5, 1.6_

- [x] 5. 实现文件保存模块
  - [x] 5.1 实现 File_Saver 核心功能
    - 实现 Excel 文件保存功能
    - 实现备份机制（保存前自动备份）
    - 实现备份管理（保留最近 5 个备份）
    - 实现覆盖确认逻辑
    - 实现从备份恢复功能
    - _需求: 2.1, 2.2, 2.3, 2.5, 2.6_
  
  - [ ]* 5.2 编写文件保存的属性测试
    - **属性 2: 保存前自动备份**
    - **验证: 需求 2.2**
    - _需求: 2.2_
  
  - [ ]* 5.3 编写备份恢复的属性测试
    - **属性 3: 保存失败自动恢复**
    - **验证: 需求 2.4**
    - _需求: 2.4_

- [x] 6. 检查点 - 确保文件加载和保存功能正常
  - 确保所有测试通过，询问用户是否有问题

### 阶段 3: 内容处理功能

- [x] 7. 实现内容处理模块
  - [x] 7.1 实现删除空白行功能
    - 识别所有单元格均为空的行
    - 删除空白行并保持其他行不变
    - _需求: 3.1_
  
  - [ ]* 7.2 编写删除空白行的属性测试
    - **属性 4: 删除空白行后无全空行**
    - **验证: 需求 3.1**
    - _需求: 3.1_
  
  - [x] 7.3 实现清除空白单元格内容功能
    - 清除空白单元格内容但保留结构
    - _需求: 3.2_
  
  - [x] 7.4 实现删除公式功能
    - 识别所有公式单元格
    - 将公式替换为计算结果值
    - 保持单元格格式不变
    - _需求: 3.3, 3.8_
  
  - [ ]* 7.5 编写删除公式的属性测试
    - **属性 5: 删除公式保留计算值**
    - **验证: 需求 3.3**
    - _需求: 3.3_
  
  - [x] 7.6 实现删除重复行功能
    - 识别完全重复的数据行
    - 支持指定关键列判断重复
    - 删除重复行并保留第一次出现的行
    - _需求: 3.4, 3.5_
  
  - [ ]* 7.7 编写删除重复行的属性测试
    - **属性 6: 删除重复行后无重复**
    - **验证: 需求 3.4**
    - _需求: 3.4_
  
  - [x] 7.8 实现按规则替换内容功能
    - 实现普通文本匹配和替换
    - 实现正则表达式匹配和替换
    - 支持大小写敏感/不敏感选项
    - 支持全词匹配选项
    - 实现替换规则验证
    - _需求: 3.6, 3.7_
  
  - [ ]* 7.9 编写内容替换的属性测试
    - **属性 7: 内容替换规则正确应用**
    - **验证: 需求 3.6**
    - _需求: 3.6_
  
  - [ ]* 7.10 编写格式保持的属性测试
    - **属性 8: 内容操作保持格式不变**
    - **验证: 需求 3.8**
    - _需求: 3.8_

- [x] 8. 检查点 - 确保内容处理功能正常
  - 确保所有测试通过，询问用户是否有问题

### 阶段 4: 图像处理功能

- [ ] 9. 实现图像处理模块
  - [x] 9.1 实现提取图片功能
    - **方案 A（推荐）**：使用 zipfile 库直接从 .xlsx 文件的 `xl/media/` 目录提取图片（更快更稳定）
    - **方案 B（备选）**：使用 openpyxl 的 `_images` 属性提取图片（兼容性可能有问题）
    - 生成唯一的文件名（工作表名_图片序号.扩展名）
    - 保存图片到指定目录
    - 处理图片格式识别和转换
    - _需求: 4.1, 4.2_
    - _最佳实践: zipfile 方案避免 openpyxl 的图片兼容性问题_
  
  - [ ]* 9.2 编写提取图片的属性测试
    - **属性 9: 图片提取完整性**
    - **验证: 需求 4.1**
    - _需求: 4.1_
  
  - [ ] 9.3 实现替换图片功能
    - 显示工作表中所有图片列表
    - 替换选中的图片
    - 保持原始位置和尺寸
    - _需求: 4.3, 4.4_
  
  - [ ]* 9.4 编写替换图片的属性测试
    - **属性 10: 图片替换保持位置**
    - **验证: 需求 4.4**
    - _需求: 4.4_
  
  - [x] 9.5 实现添加水印功能
    - 支持文字水印和图片水印
    - 支持多种水印位置选项
    - 支持透明度调节
    - 使用 Pillow 处理图片
    - _需求: 4.5, 4.6_
  
  - [ ]* 9.6 编写图像处理的单元测试
    - 测试不同图片格式的支持
    - 测试水印添加效果
    - _需求: 4.6, 4.7_

- [x] 10. 检查点 - 确保图像处理功能正常
  - 确保所有测试通过，询问用户是否有问题

### 阶段 5: 工作表管理功能

- [ ] 11. 实现工作表管理模块
  - [x] 11.1 实现插入工作表功能
    - 在指定位置插入新工作表
    - 支持自定义工作表名称
    - 检查工作表名称重复
    - _需求: 5.1, 5.2, 5.3_
  
  - [ ]* 11.2 编写插入工作表的属性测试
    - **属性 11: 工作表插入保持原有工作表**
    - **验证: 需求 5.1**
    - _需求: 5.1_
  
  - [ ] 11.3 实现删除工作表功能
    - 删除指定的工作表
    - 验证不能删除唯一的工作表
    - 提供删除确认提示
    - _需求: 5.4, 5.5, 5.6_
  
  - [ ]* 11.4 编写删除工作表的边界测试
    - **边界情况: 删除唯一工作表被拒绝**
    - **验证: 需求 5.5**
    - _需求: 5.5_
  
  - [ ] 11.5 实现重命名工作表功能
    - 修改工作表名称
    - 检查名称重复
    - _需求: 5.7_

- [ ] 12. 检查点 - 确保工作表管理功能正常
  - 确保所有测试通过，询问用户是否有问题

### 阶段 6: 文件合并与拆分功能

- [x] 13. 实现合并拆分引擎
  - [x] 13.1 实现 Excel 文件合并功能
    - 支持追加到同一工作表模式
    - 支持保留独立工作表模式
    - 实现智能表头识别和合并
    - 实现列结构一致性检查
    - _需求: 6.1, 6.3, 6.4_
  
  - [ ]* 13.2 编写文件合并的属性测试
    - **属性 12: 文件合并数据完整性**
    - **验证: 需求 6.1**
    - _需求: 6.1_
  
  - [x] 13.3 实现 CSV 文件合并功能
    - 合并多个 CSV 文件
    - 智能处理表头
    - _需求: 6.2, 6.3_
  
  - [x] 13.4 实现 Excel 文件拆分功能
    - 按指定行数拆分文件
    - 每个拆分文件保留表头
    - 使用规范的文件命名
    - _需求: 6.5, 6.7_
  
  - [ ]* 13.5 编写文件拆分的属性测试
    - **属性 13: 文件拆分往返一致性**
    - **验证: 需求 6.5**
    - _需求: 6.5_
  
  - [x] 13.6 实现 CSV 文件拆分功能
    - 按指定行数拆分 CSV 文件
    - 保留表头
    - _需求: 6.6, 6.7_

- [x] 14. 检查点 - 确保合并拆分功能正常
  - 确保所有测试通过，询问用户是否有问题

### 阶段 7: 格式转换功能

- [x] 15. 实现格式转换器
  - [x] 15.1 实现 Excel 转 PDF 功能
    - **方案 A（Windows 推荐）**：使用 pywin32 调用系统 COM 组件，启动用户本地的 Excel 进行静默转换（格式 100% 一致，无需额外依赖）
    - **方案 B（跨平台）**：检测 LibreOffice 是否安装，调用 LibreOffice 进行转换（注意：会增加安装包体积）
    - **方案 C（降级）**：将每个工作表导出为图片（PNG），然后合成 PDF
    - 支持转换范围选项（当前/所有/指定工作表）
    - 支持页面设置选项
    - _需求: 7.1, 7.2, 7.5, 7.6_
    - _最佳实践: Windows 优先使用 COM 组件，避免 LibreOffice 依赖_
  
  - [x] 15.2 实现 Excel 转 CSV 功能
    - 使用 pandas 读取 Excel 并导出为 CSV（高效且稳定）
    - 为每个工作表生成独立 CSV 文件
    - 支持转换范围选项
    - 支持 CSV 分隔符和编码选项（UTF-8、GBK、GB2312）
    - _需求: 7.3, 7.4_
  
  - [ ]* 15.3 编写格式转换的属性测试
    - **属性 14: 格式转换内容保持**
    - **验证: 需求 7.4**
    - _需求: 7.4_
  
  - [x] 15.4 实现依赖检测功能
    - 启动时检测 LibreOffice
    - 显示依赖状态
    - 提供安装指引
    - _需求: 7.5, 7.7_

- [x] 16. 检查点 - 确保格式转换功能正常
  - 确保所有测试通过，询问用户是否有问题

### 阶段 8: 文件属性管理功能

- [ ] 17. 实现属性管理器
  - [ ] 17.1 实现清理元数据功能
    - 删除作者、修改者信息
    - 删除创建日期、修改日期
    - 删除公司、主题、标签等
    - _需求: 8.1_
  
  - [ ]* 17.2 编写清理元数据的属性测试
    - **属性 15: 元数据清理完整性**
    - **验证: 需求 8.1**
    - _需求: 8.1_
  
  - [ ] 17.3 实现密码保护功能
    - 实现工作簿打开密码
    - 实现工作簿结构保护
    - 实现工作表修改密码
    - 支持保护选项配置
    - 实现密码验证和移除
    - _需求: 8.2, 8.3, 8.4, 8.7_
  
  - [ ]* 17.4 编写密码保护的属性测试
    - **属性 16: 密码保护有效性**
    - **验证: 需求 8.3**
    - _需求: 8.3_
  
  - [ ] 17.5 实现文件优化功能
    - 移除未使用的样式和格式
    - 压缩嵌入的图片
    - 移除隐藏的工作表和对象
    - 清理临时数据和缓存
    - 显示优化前后对比
    - _需求: 8.5, 8.6_

- [ ] 18. 检查点 - 确保属性管理功能正常
  - 确保所有测试通过，询问用户是否有问题

### 阶段 9: 用户界面实现

- [x] 19. 实现 Electron 前端界面
  - [x] 19.1 创建主窗口和基础布局
    - 使用 Vue.js 创建主界面组件
    - 实现功能列表展示
    - 实现导航和路由
    - _需求: 9.1_
  
  - [x] 19.2 实现文件选择组件
    - 实现文件浏览对话框
    - 支持单文件和多文件选择
    - 显示选中文件列表
    - _需求: 9.5_
  
  - [x] 19.3 实现功能面板组件
    - 为每个功能创建参数设置面板
    - 标注必填项和可选项
    - 实现表单验证
    - _需求: 9.2_
  
  - [x] 19.4 实现进度指示器组件
    - 显示当前进度百分比
    - 显示当前处理文件名
    - 支持取消操作
    - _需求: 9.3_
  
  - [x] 19.5 实现结果显示组件
    - 显示成功/错误消息
    - 显示错误原因和解决方案
    - 提供打开输出文件的功能
    - _需求: 9.4_
  
  - [x] 19.6 实现功能提示和帮助
    - 添加工具提示（Tooltip）
    - 实现首次使用提示
    - 实现快速入门向导
    - _需求: 9.6, 可用性要求_

- [x] 20. 实现状态管理
  - [x] 使用 Pinia 管理应用状态
  - [x] 创建 `src/stores` 目录
  - [x] 创建文件状态 store (`src/stores/fileStore.ts`)
  - [x] 创建操作历史 store (`src/stores/historyStore.ts`)
  - [x] 创建用户设置 store (`src/stores/settingsStore.ts`)
  - [x] 更新 `src/main.ts` 初始化 Pinia（已完成）
  - [x] 重构 `src/App.vue` 使用 stores 替代本地 refs
  - _需求: 9_

- [ ] 21. 检查点 - 确保用户界面功能正常
  - 确保所有测试通过，询问用户是否有问题

### 阶段 10: 高级功能

- [ ] 22. 实现批量操作功能
  - [ ] 22.1 实现批量文件处理
    - 对多个文件依次执行相同操作
    - 显示整体进度和当前文件进度
    - 处理失败时继续处理下一个文件
    - 显示操作摘要
    - _需求: 11.1, 11.2, 11.3, 11.4_
  
  - [ ]* 22.2 编写批量操作的属性测试
    - **属性 17: 批量操作正确性**
    - **验证: 需求 11.1**
    - _需求: 11.1_
  
  - [ ] 22.3 实现任务模板功能
    - 创建和保存任务模板
    - 应用任务模板
    - 管理任务模板列表
    - _需求: 11.5, 11.6_
  
  - [ ] 22.4 实现操作取消功能
    - 支持取消长时间操作
    - 提示是否保留部分结果
    - _需求: 11.7_

- [ ] 23. 实现操作预览与撤销功能
  - [ ] 23.1 实现操作预览
    - 显示将受影响的内容
    - 高亮显示将被修改的内容
    - 提供确认和取消选项
    - _需求: 12.1, 12.2_
  
  - [ ] 23.2 实现撤销机制
    - 创建撤销点
    - 实现撤销操作
    - 管理撤销历史（最多 10 个）
    - 文件关闭时清除撤销历史
    - _需求: 12.3, 12.4, 12.5, 12.6_
  
  - [ ]* 23.3 编写撤销功能的属性测试
    - **属性 18: 撤销往返一致性**
    - **验证: 需求 12.4**
    - _需求: 12.4_

- [ ] 24. 检查点 - 确保高级功能正常
  - 确保所有测试通过，询问用户是否有问题

### 阶段 11: 错误处理与日志

- [ ] 25. 实现错误处理系统
  - [ ] 25.1 实现统一错误处理
    - 定义所有错误代码和错误类型
    - 实现错误捕获和转换
    - 生成用户友好的错误消息
    - 包含错误上下文和解决方案
    - _需求: 10.1_
  
  - [ ]* 25.2 编写错误处理的属性测试
    - **属性 19: 错误处理不破坏原文件**
    - **验证: 需求 10.3**
    - _需求: 10.3_
  
  - [ ] 25.3 实现日志系统
    - 记录所有操作到日志文件
    - 实现日志格式化（时间戳、操作类型、结果等）
    - 实现日志脱敏处理
    - 实现日志轮转（10MB，保留 5 个文件）
    - _需求: 10.2, 10.5_
  
  - [ ] 25.4 实现日志查看功能
    - 在 UI 中显示日志
    - 支持按时间、错误类型、文件名、操作类型筛选
    - _需求: 10.4_
  
  - [ ] 25.5 实现紧急错误处理
    - 处理严重错误（内存不足、磁盘空间不足）
    - 显示紧急错误对话框
    - 安全退出应用
    - _需求: 10.6_

- [ ] 26. 检查点 - 确保错误处理和日志功能正常
  - 确保所有测试通过，询问用户是否有问题

### 阶段 12: 国际化与可用性

- [ ] 27. 实现国际化支持
  - 实现中文和英文语言切换
  - 翻译所有 UI 文本
  - 翻译错误消息
  - _需求: 可用性要求 1_

- [ ] 28. 实现键盘快捷键
  - 实现常用快捷键（Ctrl+O、Ctrl+S 等）
  - 显示快捷键提示
  - _需求: 可用性要求 2_

- [ ] 29. 检查点 - 确保国际化和可用性功能正常
  - 确保所有测试通过，询问用户是否有问题

### 阶段 13: 打包与部署

- [ ] 30. 配置打包和部署
  - [ ] 30.1 配置 PyInstaller 打包 Python 后端
    - 创建 .spec 文件
    - 配置隐藏导入和数据文件
    - **打包为独立可执行文件**：使用 `--onefile` 选项，生成单个 .exe（Windows）或可执行文件（macOS/Linux）
    - **嵌入到 Electron resources 目录**：将打包后的 Python 可执行文件放入 `app.asar.unpacked/resources/python/` 目录
    - 为 Windows、macOS、Linux 分别打包
    - 确保用户无需安装 Python 环境即可运行
    - _需求: 10_
    - _最佳实践: 避免依赖用户环境，实现开箱即用_
  
  - [ ] 30.2 配置 electron-builder 打包前端
    - 配置 electron-builder.json
    - **配置 extraResources**：确保 Python 可执行文件被正确打包到 resources 目录
    - **配置 asarUnpack**：将 Python 可执行文件排除在 asar 压缩之外，确保可以被调用
    - **配置资源路径解析**：在生产环境下使用 `process.resourcesPath` 正确定位 Python 可执行文件
    - 配置平台特定设置
    - 生成安装包（.exe、.dmg、.AppImage）
    - **测试打包后的路径解析**：确保 Electron 能在打包后正确找到 Python 可执行文件的相对路径
    - _需求: 10_
    - _最佳实践: 路径解析是打包最容易出错的环节_
  
  - [ ] 30.3 实现自动更新功能
    - 检测新版本
    - 下载和安装更新
    - _需求: 可维护性要求 3_
  
  - [ ] 30.4 创建配置文件系统
    - 实现用户配置文件
    - 支持自定义默认设置
    - _需求: 可维护性要求 4_

- [ ] 31. 跨平台测试
  - 在 Windows 10+ 上测试
  - 在 macOS 11+ 上测试
  - 在 Ubuntu 20.04+ 上测试
  - 验证一致的用户体验
  - _需求: 10_

- [ ] 32. 最终检查点 - 确保应用可以正常打包和部署
  - 确保所有测试通过，询问用户是否有问题

## 注意事项

- 标记为 `*` 的任务是可选的测试任务，可以跳过以加快 MVP 开发
- 每个任务都引用了相应的需求编号，确保可追溯性
- 检查点任务用于验证阶段性成果，确保增量开发的质量
- 属性测试使用 Hypothesis 库，每个测试至少运行 100 次迭代
- 单元测试使用 pytest，确保代码覆盖率达到 80% 以上

## 🎯 关键实施建议（避坑指南）

### 1. Python 长连接模式：避免频繁启动进程
**问题**：每次操作都启动/关闭 Python 进程会非常慢（每次启动可能需要 1-2 秒）。

**解决方案**：
- Electron 启动时启动 Python 子进程，保持长连接直到应用关闭
- Python 端使用 `sys.stdin.readline()` 循环读取命令
- **关键**：将所有日志输出到 stderr，避免干扰 stdout 中的 JSON 数据
- 参考任务 2.1 的实现细节

**Python 端示例**：
```python
import sys
import json

# 将日志输出到 stderr
def log(message):
    sys.stderr.write(f"[LOG] {message}\n")
    sys.stderr.flush()

# 主循环
while True:
    try:
        line = sys.stdin.readline()
        if not line:
            break
        
        command = json.loads(line.strip())
        log(f"Received command: {command['action']}")
        
        # 处理命令...
        result = process_command(command)
        
        # 输出结果到 stdout
        print(json.dumps(result), flush=True)
    except Exception as e:
        log(f"Error: {str(e)}")
        error_result = {"status": "error", "message": str(e)}
        print(json.dumps(error_result), flush=True)
```

### 2. 通信效率：避免 UI 假死
**问题**：Excel 处理可能耗时很长（如合并 100 个文件），如果 Electron 主进程同步等待 Python 返回，会导致界面假死。

**解决方案**：
- Python 后端应该实时通过 stdout 发送进度 JSON：`{"type": "progress", "progress": 45, "message": "正在处理第 3/10 个文件"}`
- Electron 监听 stdout 流，实时更新 Vue 组件的进度条
- 最终结果使用不同的消息类型：`{"type": "result", "status": "success", "data": {...}}`
- 参考任务 2.2 的实现细节

### 2. 通信效率：避免 UI 假死
**问题**：Excel 处理可能耗时很长（如合并 100 个文件），如果 Electron 主进程同步等待 Python 返回，会导致界面假死。

**解决方案**：
- Python 后端应该实时通过 stdout 发送进度 JSON：`{"type": "progress", "progress": 45, "message": "正在处理第 3/10 个文件"}`
- Electron 监听 stdout 流，实时更新 Vue 组件的进度条
- 最终结果使用不同的消息类型：`{"type": "result", "status": "success", "data": {...}}`
- 参考任务 2.2 的实现细节

### 3. LibreOffice 依赖：避免安装包过大
**问题**：如果依赖 LibreOffice 进行 Excel 转 PDF，会导致安装包体积巨大（几百 MB）。

**解决方案（Windows 平台）**：
- 使用 pywin32 调用系统 COM 组件，启动用户本地安装的 Excel 进行静默转换
- 优点：格式 100% 一致，无需额外依赖，安装包小
- 缺点：仅适用于 Windows，且需要用户安装了 Excel

**解决方案（跨平台）**：
- 方案 A：提示用户安装 LibreOffice（不打包在应用内）
- 方案 B：提供降级方案，将工作表导出为图片后合成 PDF
- 参考任务 15.1 的多方案实现

**pywin32 示例**：
```python
import win32com.client

def excel_to_pdf_windows(excel_path, pdf_path):
    """使用 Windows COM 组件转换 Excel 为 PDF"""
    excel = win32com.client.Dispatch("Excel.Application")
    excel.Visible = False
    excel.DisplayAlerts = False
    
    try:
        wb = excel.Workbooks.Open(excel_path)
        wb.ExportAsFixedFormat(0, pdf_path)  # 0 = xlTypePDF
        wb.Close(False)
    finally:
        excel.Quit()
```

### 4. 图片处理：使用 zipfile 提升稳定性
**问题**：openpyxl 处理嵌入式图片时有时会遇到兼容性问题。

**解决方案**：
- .xlsx 文件本质是一个 ZIP 压缩包
- 使用 Python 的 zipfile 库直接从 `xl/media/` 目录提取图片
- 这种方式更快、更稳定，且不依赖 openpyxl 的图片解析

**zipfile 提取图片示例**：
```python
import zipfile
import os

def extract_images_from_xlsx(xlsx_path, output_dir):
    """使用 zipfile 直接提取图片"""
    images = []
    
    with zipfile.ZipFile(xlsx_path, 'r') as zip_ref:
        # 列出所有文件
        for file_info in zip_ref.filelist:
            # 查找 xl/media/ 目录下的图片
            if file_info.filename.startswith('xl/media/'):
                # 提取文件名和扩展名
                filename = os.path.basename(file_info.filename)
                output_path = os.path.join(output_dir, filename)
                
                # 提取文件
                with zip_ref.open(file_info) as source:
                    with open(output_path, 'wb') as target:
                        target.write(source.read())
                
                images.append(output_path)
    
    return images
```

### 5. 环境打包：开箱即用
**问题**：打包 Python 环境是最难的环节，依赖用户安装 Python 会导致大量环境问题。

**解决方案**：
- 使用 PyInstaller 的 `--onefile` 选项，将 Python 代码打包成单个可执行文件
- 将打包后的 Python 可执行文件放入 Electron 的 `resources/python/` 目录
- 在 electron-builder 配置中使用 `extraResources` 和 `asarUnpack` 确保文件可访问
- Electron 启动时直接调用这个可执行文件，无需检测用户的 Python 环境
- 参考任务 30.1 和 30.2 的详细配置

### 5. 环境打包：开箱即用
**问题**：打包 Python 环境是最难的环节，依赖用户安装 Python 会导致大量环境问题。

**解决方案**：
- 使用 PyInstaller 的 `--onefile` 选项，将 Python 代码打包成单个可执行文件
- 将打包后的 Python 可执行文件放入 Electron 的 `resources/python/` 目录
- 在 electron-builder 配置中使用 `extraResources` 和 `asarUnpack` 确保文件可访问
- **关键**：在生产环境下使用 `process.resourcesPath` 正确定位 Python 可执行文件
- Electron 启动时直接调用这个可执行文件，无需检测用户的 Python 环境
- 参考任务 30.1 和 30.2 的详细配置

**Electron 路径解析示例**：
```typescript
import { app } from 'electron';
import path from 'path';

function getPythonExecutablePath(): string {
  if (app.isPackaged) {
    // 生产环境：从 resources 目录获取
    const platform = process.platform;
    const exeName = platform === 'win32' ? 'excel-toolkit-backend.exe' : 'excel-toolkit-backend';
    return path.join(process.resourcesPath, 'python', exeName);
  } else {
    // 开发环境：从项目目录获取
    return path.join(__dirname, '..', 'python-dist', 'excel-toolkit-backend');
  }
}
```

**electron-builder.json 配置示例**：
```json
{
  "extraResources": [
    {
      "from": "python-dist/${os}",
      "to": "python",
      "filter": ["**/*"]
    }
  ],
  "asarUnpack": [
    "resources/python/**/*"
  ]
}
```

### 6. 文件占用检测：友好的错误提示
**问题**：用户可能在 Excel 中打开文件的同时使用工具箱处理该文件，导致文件占用错误。

**解决方案**：
- 在 File_Loader 中捕获文件占用异常（`PermissionError` 或 `IOError`）
- 返回友好的错误消息：`{"status": "error", "error_code": "FILE_IN_USE", "message": "文件正在被其他程序使用，请先关闭 Excel 后重试"}`
- 在 UI 中显示明确的提示，引导用户关闭 Excel
- 参考任务 4.1 的实现细节

### 6. 文件占用检测：友好的错误提示
**问题**：用户可能在 Excel 中打开文件的同时使用工具箱处理该文件，导致文件占用错误。

**解决方案**：
- 在 File_Loader 中捕获文件占用异常（`PermissionError` 或 `IOError`）
- 返回友好的错误消息：`{"status": "error", "error_code": "FILE_IN_USE", "message": "文件正在被其他程序使用，请先关闭 Excel 后重试"}`
- 在 UI 中显示明确的提示，引导用户关闭 Excel
- 参考任务 4.1 的实现细节

**文件占用检测示例**：
```python
import openpyxl
from openpyxl.utils.exceptions import InvalidFileException

def load_excel_safe(file_path):
    """安全加载 Excel 文件，处理文件占用"""
    try:
        wb = openpyxl.load_workbook(file_path)
        return {"status": "success", "workbook": wb}
    except PermissionError:
        return {
            "status": "error",
            "error_code": "FILE_IN_USE",
            "message": "文件正在被其他程序使用，请先关闭 Excel 后重试",
            "suggested_action": "关闭 Microsoft Excel 或其他正在使用该文件的程序"
        }
    except InvalidFileException:
        return {
            "status": "error",
            "error_code": "FILE_CORRUPTED",
            "message": "文件已损坏或格式不正确",
            "suggested_action": "请检查文件是否完整，或尝试使用 Excel 修复该文件"
        }
    except Exception as e:
        return {
            "status": "error",
            "error_code": "UNKNOWN_ERROR",
            "message": f"加载文件时发生错误: {str(e)}"
        }
```

## 🛠️ 推荐技术栈清单

基于实战经验，以下是推荐的具体工具和库：

### Python 后端

**Excel 处理**：
- `openpyxl==3.1.2` - 基础读写 .xlsx 文件，支持样式和公式
- `xlrd==2.0.1` - 读取旧版 .xls 文件（Excel 97-2003）
- `xlwt==1.3.0` - 写入旧版 .xls 文件
- `pandas==2.2.0` - 高速数据处理、合并、拆分
- `XlsxWriter==3.1.9` - 高级样式和图表支持（可选）

**图像处理**：
- `Pillow==10.2.0` - 图像处理、水印添加
- `zipfile` - 标准库，用于直接从 .xlsx 提取图片

**格式转换**：
- `pywin32==306` - Windows COM 组件调用（仅 Windows）
- `subprocess` - 标准库，调用 LibreOffice（跨平台）

**测试**：
- `pytest==8.0.0` - 单元测试框架
- `hypothesis==6.98.0` - 属性测试框架
- `pytest-benchmark==4.0.0` - 性能测试

**打包**：
- `pyinstaller==6.3.0` - 打包为独立可执行文件

### Node.js / Electron 前端

**核心框架**：
- `electron==28.0.0` - 桌面应用框架
- `vue==3.4.0` - UI 框架
- `pinia==2.1.0` - 状态管理
- `typescript==5.3.0` - 类型安全

**通信**：
- `child_process` - Node.js 标准库，启动 Python 子进程

**UI 组件**：
- `element-plus==2.5.0` - Vue 3 组件库（可选）
- `vite==5.0.0` - 构建工具

**打包**：
- `electron-builder==24.9.0` - 应用打包工具

### 开发工具

**代码质量**：
- `eslint` + `prettier` - 前端代码格式化
- `black` + `flake8` - Python 代码格式化

**版本控制**：
- Git + `.gitignore` 配置

## 📦 依赖安装命令

**Python 后端**：
```bash
pip install openpyxl==3.1.2 pandas==2.2.0 Pillow==10.2.0 xlrd==2.0.1 xlwt==1.3.0
pip install pytest==8.0.0 hypothesis==6.98.0 pytest-benchmark==4.0.0
pip install pyinstaller==6.3.0

# Windows 平台额外安装
pip install pywin32==306
```

**Node.js 前端**：
```bash
npm install electron@28.0.0 vue@3.4.0 pinia@2.1.0 typescript@5.3.0
npm install vite@5.0.0 electron-builder@24.9.0
npm install --save-dev eslint prettier
```

### 4. 进度消息格式示例

**Python 后端发送进度**：
```python
import json
import sys

def send_progress(progress, message):
    """发送进度更新"""
    progress_data = {
        "type": "progress",
        "progress": progress,
        "message": message
    }
    print(json.dumps(progress_data), flush=True)

def send_result(status, data):
    """发送最终结果"""
    result_data = {
        "type": "result",
        "status": status,
        "data": data
    }
    print(json.dumps(result_data), flush=True)

# 使用示例
send_progress(25, "正在加载文件...")
send_progress(50, "正在处理数据...")
send_progress(75, "正在保存文件...")
send_result("success", {"output_file": "result.xlsx"})
```

**Electron 前端接收**：
```typescript
pythonProcess.stdout.on('data', (data) => {
  const lines = data.toString().split('\n');
  for (const line of lines) {
    if (line.trim()) {
      const message = JSON.parse(line);
      if (message.type === 'progress') {
        // 更新进度条
        updateProgress(message.progress, message.message);
      } else if (message.type === 'result') {
        // 处理最终结果
        handleResult(message);
      }
    }
  }
});
```
